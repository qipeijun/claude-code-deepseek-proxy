{
  "name": "claude-code-deepseek-proxy",
  "version": "0.1.0",
  "description": "Anthropic-compatible routing proxy for Claude Code and DeepSeek.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/index.ts --kill-port",
    "dev:safe": "tsx watch src/index.ts",
    "start": "tsx src/index.ts --kill-port",
    "start:safe": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "better-sqlite3": "^11.10.0",
    "fastify": "^5.2.1",
    "pino": "^9.6.0",
    "undici": "^7.3.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^22.13.1",
    "pino-pretty": "^13.1.3",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  },
  "engines": {
    "node": ">=22"
  }
}
